import styled from "styled-components";

export const TextStyle = styled.p`
  overflow: hidden;
  
  max-width: 100%;
  min-width: 20px;
  margin: 0;

  text-overflow: ellipsis;
  white-space: nowrap;
`;
